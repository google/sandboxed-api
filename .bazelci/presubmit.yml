---
tasks:
  debian13:
    shell_commands:
    # Install the prebundled llvm version.
    - "sudo apt -y update && sudo apt -y install llvm-dev libclang-dev libncurses-dev libzstd-dev python3-clang"
    build_targets:
    - "..."
